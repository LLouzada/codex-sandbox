# docker compose run --rm codex-agent

name: codex-sandbox
services:
  codex-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: codex-agent
    working_dir: /workspace
    volumes:
      - ${CODEX_REPO_HOST:-../../}:/workspace:rw
      - ./codex-home:/home/codex:rw
    read_only: false
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    stdin_open: true
    tty: true

  codex-agent-offline:
    extends:
      service: codex-agent
    profiles: ["offline"]
    network_mode: "none"
